---
//ICONS
import ArrowIcon from './icons/arrow-icon.astro';
//STYLES
import './header-title.css';

interface Props {
  title: string;
  isLink?: boolean;
  href?: string;
}

const { title, isLink = false, href = '/' } = Astro.props;
---

<h1 class="header-title">
  {
    isLink ? (
      <a href={href} class="header-title__link">
        <ArrowIcon iconSize={40} />
        {title}
      </a>
    ) : (
      title
    )
  }
</h1>
